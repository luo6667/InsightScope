# 舆情雷达 InsightScope · 服务端环境变量示例
# 复制为 .env 后按需修改（或直接以环境变量方式注入）

# 端口（默认 5176）
# PORT=5176

# MongoDB 连接串（生产必改，勿用默认密码）
# MONGO_URI=mongodb://user:password@127.0.0.1:27017/insight?authSource=admin

# CORS 白名单（逗号分隔，允许的前端域名；默认本地 dev 前后端）
# CORS_ORIGIN=http://localhost:5175,http://localhost:5176

# 写接口限流（次/分钟/IP；0=关闭，公网部署建议 60）
# RATE_LIMIT_PER_MIN=60

# 测试用 Mock AI 端点开关（生产建议关闭）
# ENABLE_MOCK_AI=false

# 前端构建产物目录（默认 ../web/dist，一般无需改）
# WEB_DIST=../web/dist

# 运行环境（production 时错误响应不泄露内部细节）
# NODE_ENV=production
